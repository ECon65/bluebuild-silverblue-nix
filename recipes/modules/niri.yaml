---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:

  - type: dnf
    repos:
      copr:
        enable:
          - yalter/niri
    install:
      install-weak-deps: true
      packages:
        - niri
        - noctalia
